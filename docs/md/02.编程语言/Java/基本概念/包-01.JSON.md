# JSON

## Jackson

### 必要背景

(1) SpringBoot默认集成是指已经包含了`spring-boot-starter-json`包， 具体的json包：

```xml
<dependency>
    <groupId>com.fasterxml.jackson.core</groupId>
    <artifactId>jackson-databind</artifactId>
    <version>2.12.7.1</version>
    <scope>compile</scope>
</dependency>
<dependency>
    <groupId>com.fasterxml.jackson.datatype</groupId>
    <artifactId>jackson-datatype-jdk8</artifactId>
    <version>2.12.7</version>
    <scope>compile</scope>
</dependency>
<dependency>
    <groupId>com.fasterxml.jackson.datatype</groupId>
    <artifactId>jackson-datatype-jsr310</artifactId>
    <version>2.12.7</version>
    <scope>compile</scope>
</dependency>
<dependency>
    <groupId>com.fasterxml.jackson.module</groupId>
    <artifactId>jackson-module-parameter-names</artifactId>
    <version>2.12.7</version>
    <scope>compile</scope>
</dependency>
```

（2）jsr310 以及 其他包主要解决类型升级时(比如LocalDateTime类型)的配置处理

```java
ObjectMapper mapper = new ObjectMapper();
mapper.registerModule(new ParameterNamesModule())
    .registerModule(new Jdk8Module())
    .registerModule(new JavaTimeModule());

mapper.findAndRegisterModules();  // 这也是上面的效果，但其中的代码还未看懂
```

### 注解	

| 注解                      | 作用                                                         |
| ------------------------- | ------------------------------------------------------------ |
| `@JsonProperty("name")`   | 自定义序列化字段名                                           |
| `@JsonIgnore`             | 忽略该字段                                                   |
| `@JsonIgnoreProperties`   | 类级别批量忽略                                               |
| `@JsonInclude(NON_NULL)`  | null 值不序列化                                              |
| `@JsonFormat`             | 日期/数字格式化                                              |
| `@JsonAlias`              | 反序列化时接受多个别名                                       |
| `@JsonSerialize`          | 指定自定义序列化器                                           |
| `@JsonDeserialize`        | 指定自定义反序列化器                                         |
| @JsonManagedReference     | 控制循环引用                                                 |
| @JsonBackReference        | 控制循环引用                                                 |
| @JsonValue                | 枚举输出                                                     |
| @JacksonAnnotationsInside | 内联标记<br />标记了@Sensitive 就像直接在那个元素上使用了 @JsonSerialize |

特别指出的 @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")，格式化shape + pattern	



### 常见的场景

#### (1) 时间

>在默认情况下输出的时间格式如下:
>
>​	Date			-> 2025-11-28T08:06:32.915+00:00
>
>​	LocalDateTime   -> 2025-11-28T16:06:32.9155581
>
>
>
>​	Date			<- 2025-11-28T08:06:32.915+00:00
>
>​	Date			<- 2025-11-28T16:06:32.9155581
>
>​	Date			<- 2025-11-28T16:06:32.915Z
>
>
>
>​	LocalDateTime   <- 2025-11-28T16:06:32.9155581
>
>​	LocalDateTime   <- 2025-11-28T16:06:32.915Z



通过这些方式可以控制时间输出：

1. spring.jackson.date-format = yyyy-MM-dd HH:mm:ss 

2. builder.simpleDateFormat("yyyy-MM-dd HH:mm:ss") 

​	影响Date(序列化和反序列化), 但是不影响LocalDateTime

3. @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")

​	影响Date和LocalDateTime(序列化和反序列化),

​	Date的转换宽松一些,比如 "2025-11-28 09:35:57.257" Date可以接收但是LocalDateTime不可以接收

4. 全局配置

```java
@Bean
public Jackson2ObjectMapperBuilderCustomizer jackson2ObjectMapperBuilderCustomizer() {
    return builder -> {
        builder.simpleDateFormat("yyyy-MM-dd HH:mm:ss");
        builder.serializerByType(LocalDateTime.class, 
                                 new LocalDateTimeSerializer(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")));
        builder.deserializerByType(LocalDateTime.class, 
                                   new LocalDateTimeDeserializer(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")));
    };
}
```

#### (2) 枚举

默认根据NAME进行转换, 偶尔也许有标注转换?

```java
@JsonValue
public String getCode()
{
    return code;
}
```

参考: SerializationFeature.WRITE_ENUMS_USING_TO_STRING

#### (3) LONG

```java
builder.serializerByType(long.class, new LongSerializer());	// extends JsonSerializer<Long> 
builder.serializerByType(Long.class, new LongSerializer()); // extends JsonSerializer<Long> 
```

也可以用内置的 ToStringSerializer 直接注册，不需要自定义:

```java
builder.serializerByType(Long.class, ToStringSerializer.instance);
builder.serializerByType(Long.TYPE, ToStringSerializer.instance);
```

### Springboot配置重写

- ObjectMapper
- Jackson2ObjectMapperBuilderCustomizer
- MappingJackson2HttpMessageConverter



**两种方式的区别：**

| 对比 | `Jackson2ObjectMapperBuilderCustomizer` | `MappingJackson2HttpMessageConverter` |
|------|----------------------------------------|--------------------------------------|
| 作用层级 | 定制 ObjectMapper 的构建过程 | 定制 HTTP 消息转换器本身 |
| 影响范围 | 全局（MVC + 手动注入的 ObjectMapper 均一致） | 仅影响 Spring MVC HTTP 请求/响应 |
| 侵入性 | 低，在原有基础上叠加 | 高，通常需要完全替换转换器 |
| 多个叠加 | 支持多个 Bean，按 @Order 顺序叠加 | 不适合叠加 |
| 推荐程度 | ✅ 首选 | ⚠️ 特殊场景才用 |

**创建链路：**

```
Jackson2ObjectMapperBuilderCustomizer（定制 Builder）
        ↓
Jackson2ObjectMapperBuilder（build 出）
        ↓
ObjectMapper
        ↓
MappingJackson2HttpMessageConverter
        ↓
Spring MVC HTTP 请求/响应 JSON 转换
```

Customizer 在上游影响"原材料"，Converter 在下游处理 HTTP；用 Customizer 配置后，MVC 和手动注入的 ObjectMapper 行为一致，用 Converter 新建 ObjectMapper 则两者会不一致。

`MappingJackson2HttpMessageConverter` 真正需要的场景：同一应用需要两套序列化规则（不同接口不同策略）、需要支持特殊 MediaType、对接遗留系统需要 HTTP 层特殊处理但不影响内部逻辑。

### 自定义序列化器（也参照ruoyi敏感词替换）

```java
// 示例：金额以分存储，序列化时输出为元
public class CentToYuanSerializer extends JsonSerializer<Long> {
    @Override
    public void serialize(Long value, JsonGenerator gen,
                          SerializerProvider provider) throws IOException {
        gen.writeString(new BigDecimal(value)
            .divide(new BigDecimal(100))
            .setScale(2, RoundingMode.HALF_UP)
            .toString());
    }
}

// 使用
public class Order {
    @JsonSerialize(using = CentToYuanSerializer.class)
    private Long amount; // 存储: 10000 → 序列化: "100.00"
}
```

### 序列化 / 反序列化

```java
@Autowired
ObjectMapper objectMapper;

// 对象 → JSON 字符串
String json = objectMapper.writeValueAsString(obj);

// JSON 字符串 → 对象
User user = objectMapper.readValue(json, User.class);

// JSON 字符串 → List
List<User> list = objectMapper.readValue(json,
    objectMapper.getTypeFactory().constructCollectionType(List.class, User.class));

// JSON 字符串 → Map
Map<String, Object> map = objectMapper.readValue(json, new TypeReference<Map<String, Object>>() {});
```

*直接注入 ObjectMapper Bean 使用，而不是 new ObjectMapper()，保证和全局配置一致*

### application.yml 常用配置

```yaml
spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null
```




