```bash
# 进程
ps -ef

ps -ef | grep redis		# 查询redis进程
ls -l /proc/1234/cwd	# 假设查到的PID是1234

# 内存
free -h

# 硬盘
df -h
du -sh /path/to

# 网络
netstat -ano
strace
```



```bash
# 进程
tasklist
tasklist /FI "PID eq 33532"	通常都是按PID查

# 网络
netstat -ano
```