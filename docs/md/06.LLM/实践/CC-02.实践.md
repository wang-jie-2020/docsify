1. 企鹅的海外版本优于国内版本, 海外版本可以通过Github无限白嫖(但只有1000积分); 下一步opencode、cc、codex

2. [cc-haha](src="https://github.com/wang-jie-2020/cc-haha.git") 编译过了, 搜索了点视频没有头绪去做什么

3. npm

   ```bash
   npm install -g opencode-ai
   npm install -g @anthropic-ai/claude-code
   npm install -g @openai/codex
   
   notepad $PROFILE
   
   New-Alias -Name cc -Value "claude"
   ```

4. cc -> vscode, codex -> app

5. cc-switch

   (1) 配合cc时, 未遇到问题, 注意禁用AUTOUPDATE

   (2) 配合codex时, ds4由于默认URL问题(reponses、chat)不能运行, 官网Issue有计划输出, 等待再测

6. ecc([everything-claude-code](src="https://github.com/affaan-m/everything-claude-code.git")) 和 omc([oh-my-claudecode](src="https://github.com/Yeachan-Heo/oh-my-claudecode")) 暂不实践

7. [superpowers](src="https://github.com/obra/superpowers.git")

   ```bash
   # Official Marketplace
   /plugin install superpowers@claude-plugins-official
   
   # Superpowers Marketplace
   /plugin marketplace add obra/superpowers-marketplace
   /plugin install superpowers@superpowers-marketplace
   ```

   ├─brainstorming 				  --- 创建功能/组件前的需求探索与设计

   ├─dispatching-parallel-agents 	--- 并行执行多个独立任务

   ├─executing-plans 				--- 在独立 session 中按计划执行实现，带检查点

   ├─finishing-a-development-branch  --- 实现完成后，指导合并/PR/清理

   ├─receiving-code-review 		  --- 处理 code review 反馈，验证后实施

   ├─requesting-code-review 		 --- 完成任务或合并前请求 code review

   ├─subagent-driven-development 	--- 在 session 内执行含独立任务的实现计划

   ├─systematic-debugging 		   --- 遇到 bug 或测试失败时，系统性排查

   ├─test-driven-development 		--- 实现前先写测试

   ├─using-git-worktrees 			--- 为 feature 工作创建隔离的 git worktree

   ├─using-superpowers 			  --- 每次会话开始时的引导 skill

   ├─verification-before-completion  --- 声称完成前必须跑验证，证据先于断言

   ├─writing-plans 				  --- 有 spec 后、写代码前，制定实现计划

   └─writing-skills 				 --- 创建/编辑/验证 skills

8. superpowers 实践

   (1) cc中全局安装, codex不安装, opencode是否兼容过来了不想测试了

   (2) `using-git-worktrees`似乎在卡过程, 不停在尝试执行git命令即使是在空目录

9. 只记不查的小问题

   (1) oc 宣称的兼容不一定稳定, SKILL也许可以, AGENTS有报错

   (2) GPT5.5时会有莫名错误, GPT5.3-CODEX就正常(怀疑superpowers中的某个skill引起的)

10. hooks

   git-hook 配置写在.git非常抽象，因为是默认忽略上传的. 前端项目的husky + **pre-commit**, 可以实现上传前的代码检查, dotnet-husky，java也有但似乎不必要

   cc-hook 强制的 在Agent运行过程中的命令, 比较常见的是通过钩子提醒通知审批

11. 跑通CodePilot

   理解能做到什么:

   （1）electron 同时运行web模式和pc模式

   （2）CLI工具集成 和 远程访问集成

   （3）通过GitHub Actions编译Release

   编译过程：

   (1) 在win上遇到了依赖vc++编译的问题, 有问题的包是"zlib-sync", mac上无这个问题

   (2) 有Google字体的问题, 有代码缺陷问题(无法准确描述)

   (3) 运行electron:dev时提示找不到dist-electron目录下文件, 运行electron:build之后可以了

   (4) 在公司机器上不能执行打包, 有系统依赖以及其他下载项卡住; mac上可以跑

11. MCP

    MCP简单的理解是第三方提供的接口(不是专指REST API), 此处的第三方也非专指通过网络访问的网站, 它只是工具的一个集合, 不应该包含细节

    常见的MCP包括:(1) 文件系统, EXCEL, 数据库 (2) GIT

    常见的MCP方式包括: (1) 本地进程（stdio）（npx最典型） (2) 远程服务 (HTTP/Streamable HTTP)

12. **CC优化**

    1. 也同样遇到了频繁访问文件的问题, 即使已经在全局中禁止但情况改善有限
    2. [CodeGraph](src="https://github.com/colbymchenry/codegraph") TODO
    3. [Understand-Anything](https://github.com/Lum1104/Understand-Anything) TODO

13. 个人实践方向

    (1) GitHub Trend, Action每周1次触发到issue

    (2) 图形效果

    ​	实践SKILL: (1) [frontend-slides](https://github.com/zarazhangrui/frontend-slides.git) (2)[slide-craft-skill](https://github.com/maverickgao8848/slide-craft-skill.git)

    ​	特效网站: (1) [reactbits](https://www.reactbits.dev/)

    ​	Video: (1) [hyperframe](src="https://github.com/heygen-com/hyperframes") (2) [Remotion](src="https://github.com/remotion-dev/remotion")

    (3) npx 渠道

    