### 1 安装

- cc源代码泄露 -> 也许之后也不理解的东西可以参考源码

  [cc-haha](src="https://github.com/wang-jie-2020/cc-haha.git")

- cc、codex(未安装)、opencode(MIT)

  - opencode~~全面~~兼容了CC的SKILL, commands(不兼容), agents(不兼容)

- cc的增强
  - [superpowers](src="https://github.com/obra/superpowers.git")
  - [everything-claude-code](src="https://github.com/affaan-m/everything-claude-code.git")

- npm包

  ```bash
  # opencode
  npm i -g opencode-ai
  
  # claude-code
  npm install -g @anthropic-ai/claude-code
  
  # codebuddy  -> 不再继续
  npm install -g @tencent-ai/codebuddy-code
  ```



### 2 环境配置

- CC-SWITCH 必要, 解决配置频繁切换

- 配置禁止AUTOUPDATE, 检测到了旧的就重命名找不到了, CC-SWITCH中包含

- PS终端

  ```bash
  notepad $PROFILE
  
  New-Alias -Name cc -Value "claude"
  ```



### 4 拓展

- 斜杠命令 npx cp
