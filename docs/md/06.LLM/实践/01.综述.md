### 一、提问范式

>**设计高效 Prompt 的两个关键原则:**
>
>**编写清晰、具体的指令**
>
>​	**使用分隔符清晰地表示输入的不同部分**
>
>​	**寻求结构化的输出**    
>
>​	**要求模型检查是否满足条件**    
>
>​	**提供少量示例**
>
>**给予模型充足思考时间**
>
>​	**指定完成任务所需的步骤**
>
>​	**指导模型在下结论之前找出一个自己的解法**

(1) Prompt转向了"边界定义"而不是"细节指导"

​	比如 “如何控制大模型输出的是JSON” 在以往必须通过提示词工程+正则检查+重试来解决, 但现在已经不是个问题, 模型从底层解决这个问题

​	包括思维链也是一样, 思考模式已经是默认包含的

(2) 写汇编需要精确控制每一条指令 → 写 C 仍要理解内存/指针 → 写 Python 几乎不用关心底层

​	**但"不需要写汇编"不等于"不需要编程思维"**



### 二、RAG

模型知识过时/不足已经不再是主要问题 -> (1) 训练规模增大 (2) 网络搜索工具 (3) 更长的上下文

现在的主要问题是 -> (1) 规模巨大 (2) 动态变化的知识 (3) 精确溯源和可信回答 (4) 多知识源融合推理



旧范式（Pipeline RAG）

Query → 检索 → 拼 Prompt → LLM → 输出

新范式（Agentic RAG）

Query → 判断（需要查？）→ 选择知识源 → 检索 → 评估（够不够？）→ 补充检索？→ 生成



### 三、工作流

简单来说，**Workflow 是让 AI 按部就班地执行指令，而 Agent 则是赋予 AI 自由度去自主达成目标。**



### 四、智能体

设计一个“指令模板”，告诉 LLM 它应该扮演什么角色、拥有哪些工具、以及如何格式化它的思考和行动



向量数据库采用Qdrant，图数据库采用Neo4J...

记忆的维度似乎有点广

MCP、ACP、A2A



