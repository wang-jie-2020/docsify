1. CC 配合 CC-SWITCH , 解决配置频繁需要切换的场景

2. NPM-CC 配置禁止AUTOUPDATE, 检测到了旧的就重命名找不到了

3. 每次唤起终端输入全程太麻烦了, PS中alias cc

4. superpowers 只有SKILLS, 全部拿

5. everything-claude-code(ecc) 过于全了, 但比较偏重于ts, 拿AGENTS、COMMANDS的一些

6. 引入其他调教时的方式, 比较正式的是Plugin 和 npx

   2. 尝试通过此方式安装 superpowers, 使用方式如/PLUGIN:SKILL
   3. NPX只是NODE上的包管理v2, 在实际效果可以概括为CP
   4. **试试做自己的**

7. SESSION管理
   1. history.jsonl 还是有点难找, 试过拿ID再Continue, 当时的效果不好
   2. CC-SWITCH中会有GUI
   3. **ECC提供了SESSION命令**
   
8. 项目创建

   1. 如果是已有项目, 通过AI快速输出项目整体架构文档(code-maps @code-explorer)
   2. 如果是新建项目, 通过PLAN模式询问细节, 划分阶段和实施步骤的详细文档
   3. **问题1, 需要在项目的CLAUDE.md中定义好文档结构**
   4. **问题2, 需要在项目的CLAUDE.md中明确持续迭代文档**

9. Hooks
   1. GIT --- 实现检查FORMAT?
   2. CI/CD、HOOK、GitHubActions

10. 全过程的项目

    (1) GitHub 每周热门, 仿照网络资料

    (2) 实际问题, 频繁打开文件夹、打开终端窗口, 定义一些快捷操作 (PC端Electron模板)

11. MCP、SKILLHUB、CLAW

    claw-gui: clawX poco-claw

    claude-gui: codePilot