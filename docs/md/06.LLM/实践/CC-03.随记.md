### CLI工具

1. CC 和 OpenCode，OpenCode只自动检测到了SKILL, 但其他均不可, AGENTS有报错

2. GPT5.5时会有莫名错误, GPT5.3-CODEX就正常(怀疑superpowers中的某个skill引起的)

3. 拓展方式

- 通过npx (https://skills.sh), 会下载仓库, 我测试的时候它直接下载到了.agent目录而不是.claude目录

- 通过插件, /plugin install superpowers@claude-plugins-official, 它安装了莫名的东西

  ![image-20260512105333589](https://raw.gitcode.com/qq_36179938/images/raw/main/image-20260512105333589.png)

- **CP始终可控**



### superpowers

```bash
# Official Marketplace
/plugin install superpowers@claude-plugins-official

# Superpowers Marketplace
/plugin marketplace add obra/superpowers-marketplace
/plugin install superpowers@superpowers-marketplace
```

- superpower-skills

  ├─brainstorming --- 创建功能/组件前的需求探索与设计

  ├─dispatching-parallel-agents --- 并行执行多个独立任务

  ├─executing-plans --- 在独立 session 中按计划执行实现，带检查点

  ├─finishing-a-development-branch --- 实现完成后，指导合并/PR/清理

  ├─receiving-code-review --- 处理 code review 反馈，验证后实施

  ├─requesting-code-review --- 完成任务或合并前请求 code review

  ├─subagent-driven-development --- 在 session 内执行含独立任务的实现计划

  ├─systematic-debugging --- 遇到 bug 或测试失败时，系统性排查

  ├─test-driven-development --- 实现前先写测试

  ├─using-git-worktrees --- 为 feature 工作创建隔离的 git worktree

  ├─using-superpowers --- 每次会话开始时的引导 skill

  ├─verification-before-completion --- 声称完成前必须跑验证，证据先于断言

  ├─writing-plans --- 有 spec 后、写代码前，制定实现计划

  └─writing-skills --- 创建/编辑/验证 skills

- claude-code-plugins

  ├─frontend-design

- everything-claude-code

  ├─agents

  ​	├──code-architect

  ​	├──code-explorer

  ​	├──code-reviewer

  ​	├──planner

  ├─commands

  ​	├──checkpoint

  ​	├──code-maps

  ​	├──code-review

  ​	├──save-session (配合脚本)

  ​	├──sessions (配合脚本)



### session 和 hook 管理

1. CLI模式下不好管理，偏向于GUI模式
2. 我尝试了通过history查找ID再Continue，当时的效果不好



1. git-hook

   (1) 配置在.git文件夹就非常抽象，因为是默认忽略上传的

   (2) 前端项目的husky + **pre-commit**, 可以实现上传前的代码检查

   (3) dotnet-husky，java也有但似乎不必要

2. cc-hook

   （1）强制的 在Agent运行过程中的命令

   （2）比较常见的是通过钩子提醒通知审批



### 跑通CodePilot

这个项目中有几项我想了解：

（1）electron 同时运行web模式和pc模式

（2）CLI工具集成 和 远程访问集成

（3）通过GitHub Actions编译Release



过程：

(1) 在win上遇到了依赖vc++编译的问题, 有问题的包是"zlib-sync", mac上无这个问题

(2) 有Google字体的问题, 有代码缺陷问题(无法准确描述)

(3) 运行electron:dev时提示找不到dist-electron目录下文件, 运行electron:build之后可以了

(4) 在公司机器上不能执行打包, 有系统依赖以及其他下载项卡住; mac上可以跑



### 写两个小东西

(1) github-trend

(2) electron-1



### MCP、SKILL

MCP简单的理解是第三方提供的接口(不是专指REST API), 此处的第三方也非专指通过网络访问的网站

它只是工具的一个集合, 不应该包含工作流程逻辑, SKILL中应当包含实现细节逻辑

SKILL 和 MCP 果然是不冲突(参阅资料下的文章)

常见的MCP包括:

(1) 文件系统, EXCEL, 数据库

(2) GIT

常见的MCP方式包括:

(1) 本地进程（stdio）（npx最典型） (2) 远程服务 (HTTP/Streamable HTTP)



### CC优化1

1. 过于频繁访问文件, 即使已经在全局中禁止但情况改善有限

2. [CodeGraph](src="https://github.com/colbymchenry/codegraph")，效果是在仓库中新建了DB，但如何改善上下文情况？？

   ```
   npm i -g @colbymchenry/codegraph
   
   cd your-project
   codegraph init -i
   ```

3. [Understand-Anything](https://github.com/Lum1104/Understand-Anything)

   ```bash
   /plugin marketplace add Lum1104/Understand-Anything
   /plugin install understand-anything
   ```



### 理一下思路

1. 搜索创意，实现
2. 搓个玩具



https://github.com/zarazhangrui/frontend-slides.git

